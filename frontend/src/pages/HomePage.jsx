import { Link } from 'react-router-dom'

// Шаг 1: заглушка. Данные и товары появятся позже.
function HomePage() {
  return (
    <section>
      <h1>Главная</h1>
      <p>Заглушка, Шаг 1. Здесь будет витрина: логотип, категории, немного товаров.</p>

      {/* Временные тестовые ссылки на динамические экраны (нет id/токена в меню).
          Уберутся на Шагах 2-3, когда появятся настоящие ссылки из данных. */}
      <p>Проверка динамических маршрутов (временные ссылки Шага 1):</p>
      <ul>
        <li><Link to="/product/1">Карточка товара #1</Link></li>
        <li><Link to="/order/test-token">Страница заказа (токен test-token)</Link></li>
      </ul>
    </section>
  )
}

export default HomePage
