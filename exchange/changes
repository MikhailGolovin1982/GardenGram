@client.on(events.NewMessage(chats=TARGET_CHAT))
async def handler(event):
    message_text = event.raw_text.lower()

    if any(keyword in message_text for keyword in KEYWORDS):
        sender = await event.get_sender()
        sender_name = getattr(sender, "first_name", "") or "Unknown"

        notify_text = (
            "🔔 Найдено сообщение про слот\n"
            f"👤 Автор: {sender_name}\n"
            f"💬 Текст: {event.raw_text}"
        )

        # 1) Печатаем в терминал (оставляем для отладки)
        print("\n" + notify_text)
        print("-" * 40)

        # 2) Отправляем в Saved Messages (сообщение самому себе)
        await client.send_message("me", notify_text)
